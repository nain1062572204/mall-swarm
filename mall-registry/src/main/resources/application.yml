server:
  port: 8761
spring:
  application:
    name: mall-registry
  devtools:
    restart:
      enabled: true
  freemarker:
    cache: false
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8761/eureka/


debug: true

